# 配置过程截图目录

请把“填写配置文件 / 保存 API Key 到本地文件”的截图放到这个目录中。

推荐命名：

- `config-setup-01.png`
- `config-setup-02.png`
- `config-setup-03.png`

推荐内容：

1. 创建 `.okx` 目录
2. 新建 `config.toml`
3. 把 API Key / Secret Key / Passphrase 写入配置文件
4. 保存完成后的示意图

上传完成后，我可以继续把这些图片插入：

- `docs/config-setup.md`
